# Решения по оптимизации — 2026-08-31

Каждый пункт обсуждён и одобрен владельцем машины отдельно.

**Все решения датированы и обратимы.** Это снимок на 2026-08-31, а не постоянные
правила: сервис можно вернуть в автозапуск (`brew services start <svc>`),
приложение — переустановить, дампы баз лежат в локальном бэкапе. При изменении
привычек таблица ниже не «нарушается» — она просто описывает прошлое.

| № | Тема | Решение |
|---|---|---|
| 1 | 8 осиротевших root-демонов (Turbo Boost Switcher, Macs Fan Control, cDock, GOG Galaxy, Free Download Manager, OpenVPN Connect, Kairos awdltool, CleanMyMac 4) | **Удалить** (все приложения давно снесены; бэкап plist/хелперов локально) |
| 2 | Драйвер Fresco Logic FL2000 | **Удалить** — адаптер продан |
| 2а | DisplayLink Manager | **Удалить** — адаптер продан пару месяцев назад; проверили: драйвер (вся история версий) свободно доступен на synaptics.com, при покупке нового адаптера ставится за минуты |
| 3 | TeamViewer | **Удалить целиком** штатным CLI-деинсталлятором |
| 4 | Loopback + Rogue Amoeba ACE | **Удалить** (не используется) |
| 5 | Автозапуск баз: MySQL, Redis, RabbitMQ | **Ручной режим** (`dev up/down`); ресёрч показал: живых подключений нет, Rabbit-очереди пусты, у Redis нет потребителей в коде. **PostgreSQL остаётся в автозапуске** (решение владельца). Базы `phantom` и `thehubblog` (пустой каркас старого блога на Strapi v3) удалены после локального дампа; актуальный блог живёт в MySQL (Strapi v5) |
| 6 | MongoDB 5.0 x86_64 (Rosetta) | **Оставить как есть** — проект требует именно 5.x, нативных сборок 5.0 под macOS/arm64 не существует. План Б на будущее: `mongo:5.0` (arm64) в OrbStack |
| 7 | Ночной чистильщик (janitor) | **Полный рерайт v2** в его репозитории + рабочий автозапуск (LaunchAgent вместо сломанного root-демона) |
| 8 | Amphetamine (блокировка сна) | **Не трогать** — используется осознанно |
| 9 | Чистка диска: хвосты Яндекс.Браузера, огрызок Photoshop 2025, dev-кэши, видео-обои | **Удалить**; кэши Claude (~1.5 ГБ) — чистит только janitor и только при закрытом приложении |
| 10 | `~/staging-dump`, бэкап приставки | **Не трогать**; сейвы Paradox — удалить |
| 11 | Homebrew: 79 устаревших формул | **Обновить всё, кроме mysql** (запинен — защита датадира спящих проектов) + `brew cleanup` |
| 12 | Документация | **Этот публичный репозиторий**; все бэкапы и дампы — строго локально, в git не попадают |

Дополнение: хелпер SwitchResX (приложение не найдено) — **одобрен к удалению**
отдельным решением, включён в `cleanup_root.sh` (блок 13/13).

## Общесистемные настройки (обсуждены 2026-08-31)

Применено: Power Nap выключен на батарее (`pmset -b powernap 0`), Wake on LAN
выключен (`pmset -a womp 0`). Автозапуск Adobe Creative Cloud отключён —
три пользовательских агента (com.adobe.ccxprocess, com.adobe.AdobeDesktopService,
com.adobe.CCLibrary) выгружены и переведены в постоянный disabled через
`launchctl disable gui/<uid>/<label>` (без sudo, plist'ы в /Library не тронуты).
Installer-демон com.adobe.acc.installer.v2 оставлен — нужен для обновлений.
Откат: `launchctl enable gui/<uid>/<label>` для каждого + перелогин.
Уже было сделано ранее: аналитика Apple выключена, Touch ID для sudo настроен,
Siri выключена (Apple Intelligence недоступен для аккаунта — выключать нечего).
Осознанно отклонено владельцем: Reduce motion/transparency (эстетика), ревизия
Login Items (всё используется), Spotlight-исключения (сам при случае).

## Планы на осень 2026

- После релиза macOS 27 (сентябрь) — выйти из бета-канала обновлений.
- `postgresql@14` помечен deprecated в Homebrew (EOL ветки — конец 2026):
  спланировать переезд на `postgresql@17` через dump/restore.
